# Database - Application (WebSocket via Neon Proxy)
DATABASE_URL=postgres://postgres:postgres@localhost:4444/lome_chat

# Database - Migrations (TCP direct to Postgres)
# Use 127.0.0.1 instead of localhost for reliable CI connectivity
MIGRATION_DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5432/lome_chat

NODE_ENV=development

VITE_API_URL=http://localhost:8787
