# Wrangler configuration for local development
# No Cloudflare account required - runs entirely local in wrangler dev

name = "lome-chat-api"
main = "src/index.ts"
compatibility_date = "2024-12-01"

[dev]
port = 8787
local_protocol = "http"
